<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HACKING SYSTEM...</title>
    <style>
        body { background: black; color: #00FF00; font-family: monospace; padding: 20px; line-height: 1.5; }
        .error { color: red; font-weight: bold; }
        #cursor { display: inline-block; width: 10px; height: 1.2em; background: #00FF00; animation: blink 0.7s infinite; }
        @keyframes blink { 50% { opacity: 0; } }
    </style>
</head>
<body>
    <div id="log"></div><span id="cursor"></span>
    <script>
        const log = document.getElementById('log');
        const lines = [
            "> Đang kết nối server 125.235.4.19...",
            "> Truy cập cơ sở dữ liệu HOSE...",
            "> [CẢNH BÁO] Phát hiện xâm nhập trái phép!",
            "> Đang giải mã tập tin cá nhân...",
            "> Hoàn tất 100%!",
            "----------------------------------",
            "HỆ THỐNG ĐÃ BỊ CHIẾM QUYỀN ĐIỀU KHIỂN!",
            "Lời nhắn: Tối nay đi chơi với anh nhé? ❤️",
            "----------------------------------"
        ];
        let i = 0;
        function type() {
            if (i < lines.length) {
                let p = document.createElement('p');
                if (lines[i].includes("CẢNH BÁO") || lines[i].includes("CHIẾM QUYỀN")) p.className = "error";
                log.appendChild(p);
                let j = 0;
                let interval = setInterval(() => {
                    if (j < lines[i].length) { p.textContent += lines[i][j++]; }
                    else { clearInterval(interval); i++; setTimeout(type, 400); }
                }, 30);
            }
        }
        type();
    </script>
</body>
</html>
